<Page x:Class="Medvedeva_PrivateAdertisementService.AddEditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Medvedeva_PrivateAdertisementService"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Добавление / редактирование объявления">

    <ScrollViewer VerticalScrollBarVisibility="Auto">

        <StackPanel Margin="15">

            <TextBlock Text="Название"/>
            <TextBox x:Name="TitleBox"
                     Height="30"
                     Margin="0 8 0 16"/>

            <TextBlock Text="Описание"/>
            <TextBox x:Name="DescriptionBox"
                     Height="80"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     Margin="0 8 0 16"/>

            <TextBlock Text="Цена"/>
            <TextBox x:Name="PriceBox"
                     Height="30"
                     Margin="0 8 0 16"/>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 16">
                <StackPanel Width="150">
                    <TextBlock Text="Город"/>
                    <ComboBox x:Name="CityBox" Height="30"/>
                </StackPanel>

                <StackPanel Width="150" Margin="16 0 0 0">
                    <TextBlock Text="Категория"/>
                    <ComboBox x:Name="CategoryBox" Height="30"/>
                </StackPanel>

                <StackPanel Width="150" Margin="16 0 0 0">
                    <TextBlock Text="Тип"/>
                    <ComboBox x:Name="TypeBox" Height="30"/>
                </StackPanel>

                <StackPanel Width="150" Margin="16 0 0 0">
                    <TextBlock Text="Статус объявления"/>
                    <ComboBox x:Name="StatusBox" Height="30"/>
                </StackPanel>

            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 16">
                <Image x:Name="AdImage"
                       Width="150"
                       Height="150"
                       Stretch="UniformToFill"
                       Margin="0 0 16 0"/>
                
            </StackPanel>
            <Button Content="Выбрать фото"
                    Width="150"
                    Click="ChooseImage_Click"
                    Margin="0 0 0 15"/>

            <Button x:Name="SaveButton"
                    Content="Сохранить"
                    Margin="0 20 0 0"
                    Click="Save_Click"/>

        </StackPanel>

    </ScrollViewer>
</Page>
