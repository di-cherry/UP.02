<Page x:Class="Medvedeva_PrivateAdertisementService.Pages.CompletedAdsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Medvedeva_PrivateAdertisementService.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
     Title="Завершенные объявления">

    <Grid Margin="16">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Завершенные объявления"
                   Style="{StaticResource PageTitle}"/>

        <TextBlock x:Name="TotalProfitText"
                   Style="{StaticResource SubTitle}"
                   Grid.Row="1"/>

        <ScrollViewer Grid.Row="2">
            <ItemsControl x:Name="CompletedAdsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource AdCard}">
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Image Source="{Binding ImagePath}"
                                       Width="140" Height="140"
                                       Stretch="UniformToFill"/>

                                <StackPanel Grid.Column="1" Margin="10">
                                    <TextBlock Text="{Binding Title}"
                                               FontSize="20"
                                               FontWeight="SemiBold"/>

                                    <TextBlock Text="{Binding Description}" />

                                    <TextBlock Text="{Binding Price, 
                                            StringFormat='Прибыль: {0} ₽'}"
                                               Foreground="{StaticResource AccentBrush}"/>

                                    <TextBlock Text="{Binding PostDate, 
                                            StringFormat='Дата: {0:d}'}"
                                               Foreground="{StaticResource SecondaryText}"/>

                                    <TextBlock Text="{Binding CityName}"
                                               Foreground="{StaticResource SecondaryText}"/>
                                    <TextBlock Text="{Binding CategoryName}"
                                               Foreground="{StaticResource SecondaryText}"/>
                                    <TextBlock Text="{Binding AdTypeName}"
                                               Foreground="{StaticResource SecondaryText}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

    </Grid>
</Page>