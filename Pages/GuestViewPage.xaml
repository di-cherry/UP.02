<Page x:Class="Medvedeva_PrivateAdertisementService.GuestViewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Просмотр объявлений">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryText" Color="#37474F"/>
        <SolidColorBrush x:Key="SecondaryText" Color="#78909C"/>
        <SolidColorBrush x:Key="AccentColor" Color="#FF9C1A"/>
    </Page.Resources>

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">

            <TextBlock Text="Все объявления"
                       Style="{StaticResource PageTitle}"/>

            <TextBox x:Name="SearchBox"
                     Height="30"
                     Margin="0 0 0 16"
                     ToolTip="Поиск по названию или описанию"
                     TextChanged="FilterChanged"/>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 16">

                <ComboBox x:Name="CityFilter"
                          Width="150"
                          Margin="0 0 16 0"
                          Height="30"
                          ToolTip="Город"
                          SelectionChanged="FilterChanged"/>

                <ComboBox x:Name="CategoryFilter"
                          Width="150"
                          Margin="0 0 16 0"
                          Height="30"
                          ToolTip="Категория"
                          SelectionChanged="FilterChanged"/>

                <ComboBox x:Name="TypeFilter"
                          Width="150"
                          Height="30"
                          Margin="0 0 16 0"
                          ToolTip="Тип"
                          SelectionChanged="FilterChanged"/>

                <Button Content="Сбросить"
                        Width="120"
                        Height="30"
                        Click="ResetFilters_Click"/>    


            </StackPanel>

        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="0 16 0 0">
            <ItemsControl x:Name="AdsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource AdCard}">

                            <Grid Margin="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Image Source="{Binding ImagePath}"
                                       Width="140"
                                       Height="140"
                                       Stretch="UniformToFill"
                                       Margin="0 0 16 0"/>

                                <StackPanel Grid.Column="1">

                                    <TextBlock Text="{Binding Title}"
                                               FontSize="20"
                                               FontWeight="Bold"
                                               Foreground="{StaticResource PrimaryText}"/>

                                    <TextBlock Text="{Binding Description}"
                                               Margin="0 6 0 6"
                                               TextWrapping="Wrap"
                                               Foreground="{StaticResource SecondaryText}"/>

                                    <StackPanel Orientation="Horizontal" Margin="0 6 0 0">
                                        <TextBlock Text="{Binding Price, StringFormat='Цена: {0} ₽'}"
                                                   FontWeight="Bold"
                                                   Foreground="{StaticResource PrimaryText}"/>    
                                    </StackPanel>

                                    <TextBlock Text="{Binding CityName}" Foreground="{StaticResource SecondaryText}"/>
                                    <TextBlock Text="{Binding CategoryName}" Foreground="{StaticResource SecondaryText}"/>
                                    <TextBlock Text="{Binding AdTypeName}" Foreground="{StaticResource SecondaryText}"/>

                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

    </Grid>
</Page>
